# 02 — Клиент и синхронизация МойСклад

Требования: R01, R02, R03. Волна: 2. Blocked by: 01.
Зона: packages/moysklad-client, apps/sync-worker; согласованные добавления зависимостей в эти пакеты.

Из брифа: «приступай к разработке таким образом чтобы соблюдать план который прописан там»; «дизайн пока не трогаем».

Спецификация: истории R02.3–R02.5, границы. Источники: 02_moysklad-integration.md, 18_admin-catalog-content.md жизненный цикл; схема и готовый db из interfaces.

Приёмка:
- [ ] Клиент: Bearer/gzip/пагинация/лимиты/timeout; bounded retries 429 и 5xx, 401/403 без бесконечных повторов, секреты не логируются, URL не позволяет утечку токена.
- [ ] Полный прогон каталог/категории/склады/цены идемпотентен, сохраняет человеческие правки и прежний кэш при сбое, sync_log безопасен.
- [ ] Остатки корректно различают physical/free/reserve, переносят нули, курсор после успеха, durable BullMQ задания и расписание.
- [ ] Изображения зеркалируются в S3 через конфиг без реальных доступов; ошибки не уничтожают прошлые изображения. Если невозможно локально проверить — явно сохранить непроверенный статус.
- [ ] Поведенческие тесты через транспорт/БД покрывают повтор, сбой, курсор, лимиты; фиксируются команды запуска и пустые имена необходимого конфига.

Не коммитить. Зависимости согласовать с оркестратором. Вернуть CONTRACT.

Требование D01: читать moysklad-source-check.md и spec §Уточнение D01, применять фактический контракт вместо ошибочного quantity в ТЗ.

Совместная работа: не редактировать корневые manifest/lockfile/scripts — их установку согласовать с оркестратором, изменить только собственные package.json. Передавать имена нужных переменных окружения без значений; описание конфигурации и запусков в apps/sync-worker/README.md. Полный прогон и инкремент не должны параллельно переписывать снимок: сериализация на уровне очереди/БД. Пауза 401 и безопасный sync_log нужны и в реальном runner, а не только в transport-тесте.
