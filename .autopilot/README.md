# Как читать эту папку

- `dashboard.html` — открывается сам в начале сборки; можно и двойным кликом.
  Этапы, прогресс, время, что осталось. Обновляется сам, пока сборка идёт.
  `index.html` рядом — просто переадресация на дашборд, чтобы он открывался
  по короткому адресу. Отдельно его открывать не нужно.
- `<дата>-<проект>/` — папка одной сборки. Дата в имени — день, когда сборка началась,
  поэтому папки идут по порядку сверху вниз. Суффикс `--wip` значит «ещё делается»;
  когда сборка сдана, суффикс снимается. Внутри:
  - `<дата>-brief.md` — твоя изначальная задача, слово в слово, и «Дополнения» — всё,
    что ты сказал позже. Сам текст задачи не переписывается.
  - `manifest.md` — список требований и что с каждым стало.
  - `spec.md` — спецификация.
  - `tickets/` — таски, на которые разбита сборка (если сборка мелкая, их нет).

Если сборка прервалась — скажи агенту «продолжи автопилот», он поднимет состояние отсюда.

## Прогоны

| Начат | Папка | Статус | Итог |
|---|---|---|---|
| 2026-09-03 | `2026-09-03-visteria-dev-readiness` | сдан | Оценка готовности всего плана, управление товарами и контентом сайта, управленческая отчётность, пересобранная дорожная карта и стартовый документ |
| 2026-09-05 | `2026-09-05-visteria-backend-stage1` | сдан | Бэкенд Этапа 1: схема базы и миграции, клиент МойСклад, перенос каталога и остатков с очередью, публичный каталог по HTTP, память проекта и шесть записей решений |
| 2026-09-09 | `2026-09-09-visteria-storefront-stage2` | сдан | Витрина Этапа 2: каталог с фильтрами, карточка, корзина, оформление с самовывозом и оплатой при получении; управленка со служебным входом флориста, редактором карточки, фото, бейджами, публикацией и журналом; раздел опта с заявкой, модерацией, доступом по ссылке и прайсом Excel/PDF; запись заказа в МойСклад и статусы вебхуком со сверкой; SEO и юридические страницы |
