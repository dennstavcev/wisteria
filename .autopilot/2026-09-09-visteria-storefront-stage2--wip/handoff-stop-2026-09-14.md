# Handoff: остановка автопилота 14.09.2026

Остановлено по просьбе пользователя: текущий формат работы съедает слишком много токенов из-за большого контекста проекта. Цель этого файла — зафиксировать, что принято, что лежит WIP на диске, и где следующий агент должен быть особенно осторожен.

## Принято и закоммичено в `visteria-website`

- `39a7217 feat: 05-06 редактор товара и оформление заказа`
  - Таск 05 принят с неблокирующими craft-замечаниями.
  - Таск 06 принят.
- `1d8a519 feat: 08 запись заказа в МойСклад`
  - Таск 08 принят после второго repair.
  - Проверки перед принятием: `orders.test.ts` 5/5, `sync-worker` 19/19, `moysklad-client` 3/3, typecheck/build по затронутым пакетам.

## Не принято

### Таск 07 — опт

Статус: `failed`, два repair исчерпаны. Код 07 есть на диске как WIP, но не принят и не закоммичен.

Блокеры:

- В API, витрине и тестах остался неподтверждённый SLA «в течение двух рабочих дней».
- Production-конфигурация минимального заказа не подключена.
- PDF-прайс ломает кириллицу через fallback в `?`.
- Экспорт молча отдаёт только первую страницу/100 строк.
- `approve+token` не атомарны.
- Часть SQL для опта всё ещё живёт в API-слое вместо `packages/db`.

Последние локальные проверки WIP 07 проходили (`wholesale.test.ts` 4/4, API typecheck/build), но ревью осталось blocking, поэтому это не критерий готовности.

### Таск 09 — статусы заказа

Статус: `repair`, repair был прерван лимитом до нового ревью. Код 09 есть на диске как WIP, но не принят и не закоммичен.

Блокеры первой версии и требования к repair:

- Не угадывать названия/alias статусов МойСклад; нужен конфиг-маппинг по реальным ID/именам.
- Контракт webhook должен соответствовать реальному МойСклад: `requestId` в query и payload с `meta/action`, а не синтетический HMAC/event shape.
- Polling должен получать state реальным способом, например через `expand=state`, и проходить все связанные заказы, а не первые 100 навсегда.
- Обработка webhook/status update должна быть атомарной и идемпотентной.
- Unknown events нужно записывать/считать, но не менять статус заказа.
- SQL по статусам лучше вынести в `packages/db`, чтобы не плодить слой данных в API/worker.

## Dirty state в кодовом репозитории

`C:/Users/Denn/claude_projects/visteria-website` оставлен грязным намеренно: это WIP 07/09 и служебные tmp-файлы. Ничего из этого не коммитить как готовое без нового узкого ревью.

На момент остановки `git status --short` показывал:

```text
 M apps/admin/src/api.ts
 M apps/admin/src/app.tsx
 M apps/admin/src/styles.css
 M apps/api/src/app.ts
 M apps/sync-worker/src/orders/index.ts
 M apps/sync-worker/src/queue.ts
 M apps/sync-worker/src/runner.ts
 M apps/sync-worker/test/orders.test.ts
 M apps/sync-worker/test/queue.test.ts
 M packages/db/src/index.ts
 M packages/db/src/wholesale.ts
 M packages/shared-types/src/index.ts
?? apps/admin/src/wholesale/
?? apps/api/src/webhooks/
?? apps/api/src/wholesale/
?? apps/api/test/webhooks.test.ts
?? apps/api/test/wholesale.test.ts
?? apps/sync-worker/test/order-status.test.ts
?? apps/web/src/app/opt/
?? packages/db/src/order-status-map.ts
?? tmp-codex-geteuid.cjs
?? tmp-codex-osuser.cjs
```

## Рекомендация по перезапуску процесса

Не продолжать этот же гигантский контекст. Следующий заход лучше делать маленькими изолированными задачами:

1. Сначала принять решение по dirty WIP: сохранить в отдельную ветку/патч для разбора или удалить после явного разрешения пользователя.
2. Запускать новый Codex/Claude task на один тикет, максимум на одну подсистему.
3. В prompt давать ссылки на файлы и короткий handoff, а не вставлять полные логи/тесты/ревью.
4. Для 07 и 09 начинать с контракта и тестов, а не с UI: реальные статусы/вебхуки МойСклад и конфигурация опта должны быть источником правды.
5. Коммитить только после отдельного зелёного ревью, один логический тикет — один коммит.

## Оптимизация контекста 14.09.2026

Корневой `AGENTS.md` сокращён до маршрутизатора, а прежний полный текст сохранён в
`docs/agent-context/project-memory-2026-09-14.md`. Общий контракт этапа разделён на модули в
`interfaces/`; активные тикеты 07, 09 и 10 перечисляют свой минимальный набор. При продолжении
читать `interfaces.md`, обязательный `interfaces/00-core.md` и только модули из тикета.
`interfaces/archive-2026-09-14.md` хранит историю и в рабочий контекст не входит.

## Продолжение 20.09.2026

- Dirty WIP 07/09 был 15.09 снят в ветку `stage2-wip` (коммит `040c8eb`) и в stash-бэкап; master остался на `1d8a519`.
- Таск 09 принят: свежее Manifest+Spec ревью чистое, blocking нет; код отделён от опта и
  закоммичен на master как `87b9852`. Замечания уровня craft — в `state.js` → `concerns`.
- `.env.example` дополнен именами `MOYSKLAD_ORGANIZATION_ID`, `ORDER_PUSH_RECOVERY_INTERVAL_MS`,
  `MOYSKLAD_WEBHOOK_TOKEN`, `MOYSKLAD_ORDER_STATUS_MAP`, `ORDER_STATUS_POLL_INTERVAL_MS`.
- Осталось: таск 07 (опт) — перезапуск с контракта и тестов, WIP только в `stage2-wip`;
  таск 10 — закоммичен, но ревью не проходил. Каждый — в отдельном контексте.
- Таск 10 принят 20.09.2026: ревью нашло два блокера (ссылки подвала на несуществующие
  `/oferta`, `/privacy`; нет ссылок на оформлении заказа), ремонт `843abe1`, перепроверка чистая.
  Остался только таск 07.
